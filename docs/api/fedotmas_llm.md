# fedotmas_llm

The LLM extension: the `agent` atom, the `PromptRule` rule, the `LLM` seam, and the `PydanticAI` backend.

## fedotmas_llm

::: fedotmas_llm

## fedotmas_llm.adapters.pydantic_ai

::: fedotmas_llm.adapters.pydantic_ai
