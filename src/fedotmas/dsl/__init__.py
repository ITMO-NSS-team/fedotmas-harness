"""The text DSL: the meta-agent's surface. A textual expression over a catalog of atoms,
validated and compiled into SDK Flow objects. The catalog is both the architect's menu and
the compiler's symbol table; diagnostics feed errors back in DSL vocabulary.
"""
